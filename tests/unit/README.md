Rust unit tests live next to the code they exercise under `src/`.

This directory is reserved for integration-style unit tests that need to live under
`tests/`, keeping the Orchard test layout aligned with `unit/`, `functional/`,
`golden/`, and `project/`.
